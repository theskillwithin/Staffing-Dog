import React from 'react'

import theme from './theme.css'

class Exceptions extends React.Component {
  state = {
    form: {},
  }

  render() {
    return <div className={theme.root}>exceptions....</div>
  }
}

export default Exceptions
